import { FC } from 'react';

const Main: FC = () => {
  return (
    <div className={"page flex align-center justify-center flex-col gap-5"}>
        <h1 className={'text-center text-[32px]'}>Welcome to kursa4!</h1>
    </div>
  );
};

export default Main;
